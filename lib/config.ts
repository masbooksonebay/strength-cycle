// Master UI toggle for in-app purchases.
// When false: paywall UI is hidden and all gated features behave as unlocked.
// IAP infrastructure (SKU definitions, purchase logic, gate flag types) is preserved.
// Flip back to true to re-enable the Purchase section and per-feature paywalls.
export const SHOW_IAP_UI = false;
