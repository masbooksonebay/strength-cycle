// App-wide constants shared across screens.

// Support inbox surfaced by Settings → Send Feedback. Kept here (rather than
// inline) so the address has a single source of truth.
export const FEEDBACK_EMAIL = "strengthcyclestudios@gmail.com";
